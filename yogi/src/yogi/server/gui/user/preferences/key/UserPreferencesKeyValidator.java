package yogi.server.gui.user.preferences.key;

import yogi.server.gui.record.key.KeyValidator;

public class UserPreferencesKeyValidator extends KeyValidator<UserPreferencesKey> {

	@Override
	public boolean validate(UserPreferencesKey key) {
		return super.validate(key);
	}
		
}
