package yogi.server.gui.report.key;

import yogi.server.gui.record.key.KeyValidator;

public class ReportKeyValidator extends KeyValidator<ReportKey> {
	
	@Override
	public boolean validate(ReportKey key) {
		return super.validate(key);
	}
	
}
